if ENV['GOOGLE_PLAY_JSON_KEY'] && !ENV['GOOGLE_PLAY_JSON_KEY'].empty?
  json_key_data(ENV['GOOGLE_PLAY_JSON_KEY'])
else
  json_key_file("./fastlane/playstore-credentials.json") # Local fallback for developers
end
package_name("com.rn_ci_cd") # e.g. com.krausefx.app
